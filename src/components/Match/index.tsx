import { Button, Typography } from "@mui/material"
import Grid from "@mui/material/Unstable_Grid2"

export const MatchComponent = () => {
  return (
    <Grid>
      <Button variant="contained" >Encontrar patida</Button>
    </Grid>
  )
}